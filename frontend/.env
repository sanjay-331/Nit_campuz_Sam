#VITE_BASE_URL="http://localhost:5000"
VITE_BASE_URL="https://nit-campuz-backend.onrender.com"
# GEMINI_API_KEY has been moved to the backend .env (Railway env variable).
# Never put it here — VITE_ variables are bundled into the browser JS bundle.
